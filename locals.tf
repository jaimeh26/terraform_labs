locals {
  common_tags = {
    Owner       = "YourName"   # Update with the appropriate owner name
    Environment = "Production"  # Update with the appropriate environment
    # Add any other common tags as needed
  }
}
